# API keys para AI ranking (opcional)
# Gemini:    https://aistudio.google.com  (free tier: 20 req/dia para gemini-2.5-flash)
# OpenRouter: https://openrouter.ai/keys  (free tier: depende del modelo)
#
# NOTA: Si llenas el campo de API key en la GUI (pestaña Configuración),
# SOBREESCRIBE la variable de entorno para esta DB. Si lo dejas vacío,
# se usa esta variable.
GEMINI_API_KEY=
OPENROUTER_API_KEY=
